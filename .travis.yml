language: node_js

cache: npm

node_js:
  - stable

install:
  - npm ci


jobs:
  include:
    - stage: Compile
      script: npm run build-linux
    - stage: Test
      script: npm run test

after_script:
  - npm run ci-process-cover
